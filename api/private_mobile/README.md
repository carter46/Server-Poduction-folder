Private APK store — not publicly URL-accessible.

- `banking-companion.apk` — written only by `Mobile_app/scripts/publish-apk.ps1` after a successful validated build
- `apk-meta.json` — version / built_at / file_size

Download via admin session: `api/mobile_apk_download.php`
